type: charm
name: test-worker-k8s
title: Worker tester charm
summary: Foo
description: Baz

requires:
  cluster:
    interface: cluster
    limit: 1

containers:
  workload:
    resource: workload-image

resources:
  workload-image:
    type: oci-image
    description: OCI image for dummy workload
    upstream-source: docker.io/alpine/alpine:3.20

config:
  options:
    role-all:
      type: boolean
      default: true
      description: some dummy role
    role-read:
      type: boolean
      default: false
      description: some dummy role
    role-write:
      type: boolean
      default: false
      description: some dummy role
    role-querier:
      type: boolean
      default: false
      description: some dummy role
    role-interrogator:
      type: boolean
      default: false
      description: some dummy role
    role-ingester:
      type: boolean
      default: false
      description: some dummy role
    role-compactor:
      type: boolean
      default: false
      description: some dummy role
    role-metrics_generator:
      type: boolean
      default: false
      description: some dummy role
